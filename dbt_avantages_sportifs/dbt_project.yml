name: 'dbt_avantages_sportifs'
version: '1.0.0'
config-version: 2

profile: 'dbt_avantages_sportifs'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_avantages_sportifs:
    staging:
      +materialized: view
    finance:
      +materialized: table

vars:
  working_days_per_year: 251
